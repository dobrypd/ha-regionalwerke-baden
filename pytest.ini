[pytest]
testpaths = tests
# The tests import `custom_components.*`, so the repo root must be importable.
# Without this only `python -m pytest` works (it adds the CWD); bare `pytest` fails.
pythonpath = .
asyncio_mode = auto
markers =
    integration: hits the live RWB portal; excluded from CI (pytest -m "not integration")

version: 2
updates:
  # The workflows pin actions by commit SHA, so this is what keeps them current.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
